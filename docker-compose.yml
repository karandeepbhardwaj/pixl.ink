services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - uploads:/app/uploads
      - db:/app/data
    environment:
      - PORT=8080
      - BASE_URL=http://localhost:8080
      - UPLOAD_DIR=/app/uploads
      - DB_PATH=/app/data/pixlink.db

volumes:
  uploads:
  db:
